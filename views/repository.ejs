<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= repository %></title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
        }

        header {
            background-color: #07a607;
            color: #fff;
            padding: 20px;
            text-align: center;
        }

        header h1 {
            font-size: 36px;
        }

        section {
            background-color: #fff;
            margin: 20px;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
        }

        h2 {
            color: #07a607;
            font-size: 24px;
            margin-bottom: 10px;
        }

        p {
            font-size: 18px;
            margin-bottom: 10px;
        }

        a {
            display: inline-block;
            padding: 10px 20px;
            background-color: #07a607;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            margin-right: 10px;
        }
    </style>
</head>
<body>
    <header>
        <h1><%= repository.name %></h1>
    </header>

    <section>
        <h2>Repository Details</h2>
        <p>Created Date: <%= repository.createdAt %></p>
        <p>Owner: <%= repository.owner %></p>
    </section>

    <section>
        <h2>Actions</h2>
        <a href="/viewFiles/<%= repository._id %>">View Files</a>
        <a href="/repositorySettings/<%= repository._id %>">Settings</a>
        <!-- Add other necessary actions here -->
    </section>
</body>
</html>
